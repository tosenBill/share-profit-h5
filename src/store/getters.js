const getters = {
  userInfo: (state) => state.userInfo,
  sidebar: (state) => state.sidebar,
  tabIndex: (state) => state.tabIndex,
  userDefaultDomain: (state) => state.userDefaultDomain,
  userType: (state) => state.userType
}

export default getters
